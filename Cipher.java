// Cipher is a public abstract class that contains one abstract method of type String.
// The method exists to be overridden in subclasses so that the method will print
// the kind of cipher being utilized.

public abstract class Cipher {
    public abstract String cipherType();
}
